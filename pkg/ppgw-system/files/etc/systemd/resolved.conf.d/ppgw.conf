# Free up port 53 for the clash DNS listener and keep /etc/resolv.conf
# pointing at the real upstream servers learned from DHCP.
# /etc/resolv.conf is symlinked to /run/systemd/resolve/resolv.conf by
# install.sh, so the ppgw scripts can read genuine nameserver entries.
[Resolve]
DNSStubListener=no
